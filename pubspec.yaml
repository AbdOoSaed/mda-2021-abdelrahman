name: gas_station_finder
description: This is the IMPARGO test task for the Product Engineer (Mobile) position.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  geolocator: ^7.7.1
  http: ^0.13.4
  flutter_screenutil: ^5.0.0+2
  fluttertoast: ^8.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  mockito: ^5.0.16
  build_runner: ^2.1.4

flutter:
  uses-material-design: true

